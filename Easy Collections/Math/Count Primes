class Solution:
    def countPrimes(self, n: int) -> int:
        prime = [True] * n
        prime[:2] = [False, False]
        for i in range(2, int(n**0.5)+1):
            if prime[i]:
                prime[i*i:n:i] = [False] * len(prime[i*i:n:i])
        return sum(prime)
