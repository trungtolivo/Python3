class Solution:
    def missingNumber(self, nums: List[int]) -> int:
        nums1 = sorted(nums)
        for i in range(0,len(nums)):
            if i != nums1[i]:
                return i
            else:
                pass
        return len(nums)
