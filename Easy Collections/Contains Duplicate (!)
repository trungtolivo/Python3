class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        if len(nums) < 2:
            return False
        temp = sorted(nums)
        for i in range(len(temp)):
            if temp[i] == temp[i-1]:
                return True
